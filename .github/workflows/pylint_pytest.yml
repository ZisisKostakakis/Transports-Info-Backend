name: pylint_pytest

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID  }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Docker pull from Amazon ECR - Not specifying ECR Repository or image tag as we only have one
      env:
        ECR_REGISTRY: ${{ secrets.AWS_REGISTRY_URL }}
      run: |
        docker pull $ECR_REGISTRY
        docker run $ECR_REGISTRY

    # - name: Docker run on GitHub Actions - Not specifying ECR Repository or image tag as we only have one
    #   env:
    #     ECR_REGISTRY: ${{ secrets.AWS_REGISTRY_URL }}
    #   run: |
    #     docker run $ECR_REGISTRY
